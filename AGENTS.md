# 角色公共模块

- V4 后台只维护 `admin-intents.yaml`，完整可执行 Spec 从已验证的 `api-docs/` 和项目意图确定性编译。
- 禁止新增 `admin-web.yaml`、`AdminWebHint`、旧管理菜单/spec 路由或浏览器端请求代理。
- 角色层级、状态和权限绑定属于真实后台能力；接口、约束、意图和测试必须同一次改动保持一致。
- 没有已验证的批量查询合同时，后台只能显示真实 ID，不能伪造跨模块关联名称。
